#include <stdlib.h>

int main(int agrc, char** argv) {
  int b, n, m;
  b = atoi(argv[1]);
  m = atoi(argv[1]);
  n = atoi(argv[3]);

  if (b) {
    for (int i = 0; i < n; i++) {
    }
  } else {
    for (int i = 0; i < m; i++) {
    }
  }

  return 0;
}